<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:ToDoV3"
             x:Class="ToDoV3.MainPage">

    <StackLayout x:Name="LayOutStack">
        <Label Text="ToDo app" VerticalOptions="Center" Font="calibri"/>
        <StackLayout x:Name="ControlStack" VerticalOptions="Center"></StackLayout>
        <StackLayout x:Name="ToDoStack" VerticalOptions="Center"></StackLayout>
        <!--<ListView x:Name="ItemListView" HorizontalOptions="FillAndExpand" VerticalOptions="FillAndExpand">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <TextCell Text="{Binding Title}" Detail="{Binding Date}" />
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>-->
    </StackLayout>

</ContentPage>
